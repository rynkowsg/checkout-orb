version: 2.1

description: >
  The orb provides advanced checkout features, including:
  - LFS,
  - submodules,
  - providing custom SSH keys,
  - specifying fetch depth,
  - checkouts of multiple branches within the same job (by defining multiple steps).
# What will your orb allow users to accomplish?
# Descriptions should be short, simple, and informative.

# This information will be displayed in the orb registry and is not mandatory.
display:
  home_url: "https://github.com/rynkowsg/checkout-orb"
  source_url: "https://github.com/rynkowsg/checkout-orb"

# If your orb requires other orbs, you can import them like this. Otherwise remove the "orbs" stanza.
# orbs:
#  hello: circleci/hello-build@0.0.5
